
# Harness

Layout detection anchor:
- If `core/harness.md` exists → source layout (harness-kit is the root)
- If `harness-kit/core/harness.md` exists → deployed layout (harness-kit is a subdirectory)

Rules: see `core/routing.md`.
